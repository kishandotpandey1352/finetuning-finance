services:
  finance-ai-platform:
    build: .
    container_name: finance-ai-platform
    volumes:
      - .:/app
    env_file:
      - .env
    ports:
      - "8000:8000"
    command: uvicorn src.api.main:app --host 0.0.0.0 --port 8000 --reload