global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "finance-ai-platform"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["finance-ai-platform:8000"]

  - job_name: "prometheus"
    static_configs:
      - targets: ["prometheus:9090"]